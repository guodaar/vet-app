import React from "react";
import styled from "styled-components";
import { mainFont } from "../../const/colors";

const MainContainer = styled.div`
  padding: 20px 10vw;
  color: ${mainFont};
`;

export default MainContainer;
